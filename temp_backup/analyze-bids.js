const XLSX = require("xlsx"); const fs = require("fs"); const path = require("path"); const bidsFolder = path.join(__dirname, "../.qodo/CRE EX Folder"); const files = fs.readdirSync(bidsFolder).filter(f => f.endsWith(".csv")); console.log("Found bid files:", files); const bids = files.map(file => { const filePath = path.join(bidsFolder, file); const workbook = XLSX.readFile(filePath); const sheet = workbook.Sheets[workbook.SheetNames[0]]; return XLSX.utils.sheet_to_json(sheet); }); console.log("Processed bids:", JSON.stringify(bids, null, 2));
