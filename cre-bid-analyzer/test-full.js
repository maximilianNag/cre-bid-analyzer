const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient({ datasources: { db: { url: "postgresql://postgres:postgres@localhost:5432/cre_bid_analyzer?schema=public" } } }); async function main() { try { const project = await prisma.project.create({ data: { name: "Office Building Renovation", description: "Complete renovation of a 10-story office building", ownerId: "00000000-0000-0000-0000-000000000000", settings: { fuzzyMatchThreshold: 80, priceDeviationWarning: 5, priceDeviationError: 10 } } }); console.log("Created project:", project); const contractor = await prisma.contractor.create({ data: { name: "ABC Construction", contactInfo: { email: "contact@abc-construction.com", phone: "555-0123" } } }); console.log("Created contractor:", contractor); await prisma.$disconnect(); } catch (error) { console.error("Error:", error); await prisma.$disconnect(); } } main();
