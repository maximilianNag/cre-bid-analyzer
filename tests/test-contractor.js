const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { try { const contractor = await prisma.contractor.create({ data: { name: "ABC Construction", contactInfo: { email: "contact@abc-construction.com", phone: "555-0123" } } }); console.log("Created contractor:", contractor); const contractors = await prisma.contractor.findMany(); console.log("All contractors:", contractors); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } main();
